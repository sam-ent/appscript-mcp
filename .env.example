# Google Automation MCP Configuration
# Copy this file to .env and configure as needed

# Optional: Path to Google OAuth client secret JSON file
# Default locations checked: ~/.config/google-automation-mcp/client_secret.json, ./client_secret.json, ~/.secrets/client_secret.json
# GOOGLE_CLIENT_SECRET_PATH=/path/to/client_secret.json

# Optional: Directory to store credentials (token cache)
# Default: ~/.config/google-automation-mcp
# GOOGLE_CREDENTIALS_DIR=/path/to/credentials/dir
